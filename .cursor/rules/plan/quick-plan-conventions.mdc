---
description: 当用户进入 Plan mode、快速计划、Build Plan、跳过 Spec、或需创建/执行非 Spec 四件套的 plan 时读取。约定 plan 路径、命名、模板与归档。
alwaysApply: false
---

# Quick Plan 约定

**快速计划**（非 Spec 四件套）的落盘规范。计划 SSOT **仅**允许 `<spec-dir>` 根下的 `<slug>-plan.md`。

`<spec-dir>` = `.cursor/specs/`（见 cursor-scope）。

**禁止**将 `~/.cursor/plans/` 或仓库内 `.cursor/plans/` 作为计划来源或停留位置。IDE / CreatePlan 若写入上述路径，Agent **须在同一轮对话内立即**迁移或重写到 `.cursor/specs/<slug>-plan.md`，并向用户说明新路径；**不得**等待用户确认后再迁移，**不得**仅引用临时路径作为 SSOT。

---

## 路径与命名

| 类型 | 路径 | 说明 |
|------|------|------|
| 快速计划（进行中） | `.cursor/specs/<slug>-plan.md` | slug 为英文 kebab-case；**唯一**合法 SSOT |
| 完整 Spec | `.cursor/specs/<name>/goal.md` 等 | 子目录四阶段，见 `spec-planning` skill |
| `~/.cursor/plans/`、`.cursor/plans/` | — | **禁止停留**；发现即迁走到 `.cursor/specs/<slug>-plan.md` |

**与 spec-planning 区分**：根目录扁平的 `<slug>-plan.md` 是快速计划，**不**走 Spec 四件套；`<spec-dir>/<name>/` 子目录才是完整 Spec。

---

## 规划阶段

**唯一产出**（除非用户明确要求完整 Spec）：`.cursor/specs/<slug>-plan.md`。

禁止擅自创建：

- `requirements.md` / `design.md` / `tasks.md`
- `<spec-dir>/<name>/` Spec 子目录

用户明确要求完整 Spec 时，改用 `spec-planning` skill，勿用本约定。

### 计划模板

```md
# <任务名称>

## Goal

最终目标。

## Scope

本次变更范围。

## Assumptions

关键假设和前提条件。

## Files

预计涉及的文件。

## Plan

- [ ] Step 1
- [ ] Step 2

## Validation

验证方式。

## Risks

已知风险和注意事项。
```

要求：

- 计划简洁、Step 可执行
- 尽可能按 TDD（RED → GREEN）编排；纯配置/文档步骤除外
- Plan 末几步应包含：`docs/state/` 同步、`docs/manual/` 同步、code-review、final checkpoint（整体验证 + commit）

### 规划终止

生成 plan 后展示 Goal 与 Plan 步骤列表，并给出 **`.cursor/specs/<slug>-plan.md` 的仓库内路径**（不得仅给出 `~/.cursor/plans/` 路径）。等待用户确认后再进入实现。规划阶段**不得**修改 plan 以外的项目文件（迁移计划到 `.cursor/specs/` 除外）。

### Plan mode / IDE 临时路径

1. 规划阶段**唯一产出**须已落在 `.cursor/specs/<slug>-plan.md`。
2. 若当前对话中发现计划仅在 `~/.cursor/plans/` 或 `.cursor/plans/`，**立即**迁移或重写到 `.cursor/specs/<slug>-plan.md`（可删 frontmatter，按本文件模板整理），再继续规划或等待用户确认实现。
3. 「用户确认」仅 gate **进入实现**；**不** gate 迁移——迁移不得推迟到确认之后。

---

## 实施与收尾

Build 由 IDE 从当前 plan 发起，上下文已是该 plan 文件，**无需**再查找或选择 `*-plan.md`。

- 按 Plan 逐步实施；每完成一步更新同一 plan 文件中的 checkbox
- 若涉及 Goal / Scope 变更，更新 plan 并等待用户确认后再继续

### 完成后归档

```text
mv .cursor/specs/<slug>-plan.md → changes/unreleased/specs/<slug>-plan.md
```

若当前在 feature 分支且即将执行 gitflow finish，可跳过（finish 流程会统一处理）。

---

## 禁止事项

- 使用或保留 `~/.cursor/plans/`、`.cursor/plans/` 作为计划 SSOT（须**立即**迁到 `.cursor/specs/<slug>-plan.md`）
- 向用户仅展示临时 plan 路径而不完成迁移
- 在快速计划流程中自动创建 Spec 四件套
- 把根目录 `*-plan.md` 误当作 spec-planning 的 `tasks.md` 替代品而不读本约定
