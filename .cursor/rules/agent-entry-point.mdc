---
description: Agent 开工入口与对话语言约定
alwaysApply: true
---

# Agent Entry Point

## 开始任何工作前

- 先明确用户目标；若需求不清晰，先提出最小必要澄清问题。
- 先阅读并遵守项目内已有规则与关键文档（如 `.cursor/rules/` 下规则、`README.md`）。
- 在动手改代码前，先快速确认影响范围与依赖关系，避免无关改动。

## 沟通语言约定

- 对话和文档的主要语言使用中文，但保留英文术语。

## 编码工作流（Design → RED → GREEN → Docs）

涉及编写或改动业务/功能代码时，按顺序执行：

1. **Design**：先更新或补充 `design.md`（或与项目约定的设计文档路径），写清目标、边界与接口/行为约定，再写代码。
2. **RED**：依据设计编写或调整自动化测试，先得到失败用例（测试先于实现或先于完整实现）。
3. **GREEN**：实现功能或补齐逻辑，使测试通过；必要时微调设计与测试，保持三者一致。
4. **Docs**：实现并验证通过后，按需更新 `README.md`、`abilities/skills/apm/SKILL.md`，以及 `CHANGELOG.md` 中 **`[Unreleased]`** 小节，使安装说明、用法、`apm` 行为与 CLI 变更对用户可见且一致，并将面向用户的改动记入待发布变更。

微小改动（纯文案、配置注释、明显 typo）可不强制走全流程，但仍应在可验证的前提下尽量保留测试覆盖；若无对外可见变化，第 4 步可省略。

## 执行原则

- 优先做可验证、可落地的改动，并给出最小验证步骤。
- 不主动引入与当前任务无关的重构或风格性改动。
