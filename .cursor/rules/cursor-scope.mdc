---
description: Cursor 平台专属规则，始终生效
alwaysApply: true
---

# Cursor Agent Rules

## 规则预加载

**MUST**:
- 每个对话必须先载入 AGENT 规则文件 `/AGENTS.md`
- 每个对话必须先载入项目基础文件 `/PROJECT.md`



## 项目目录结构

| 目录 | 定位 | 详细规则 |
|------|------|---------|
| `/README.md` | 项目对外介绍（面向用户的功能概述与快速上手） | — |
| `/CHANGELOG.md` | 面向用户的版本摘要 | — |
| `/docs/` | 活跃文档（state/manual/notes/proposals） | `docs/README.md` |
| `/changes/` | 归档（已完成文档按版本组织） | `changes/README.md` |
| `/issues/` | 缺陷管理 | `issues/README.md` |
| `/.cursor/specs/` | 即 `<spec-dir>`：子目录为进行中 Spec；根下 `<slug>-plan.md` 为快速计划 | — |
| `/.cursor/rules/` | Cursor agent 规则 | — |
| `/.cursor/skills/` | Cursor agent skills | — |
| `/.cursor/agents/` | Cursor agent 定义 | — |
| `/.cursor/hooks/` | Cursor agent hooks | — |

## Plan mode / 快速计划

进入 **Plan mode** 时，参考 `.cursor/skills/quick-plan/`。
进入 **Build Plan** 时，参考 `.cursor/skills/build-plan/`。
计划 SSOT **仅**为 `.cursor/specs/<slug>-plan.md`。
`~/.cursor/plans/` 与 `.cursor/plans/` **禁止**作为计划来源；若 IDE 写入该处，须在同一轮对话内立即迁到 `.cursor/specs/`。
